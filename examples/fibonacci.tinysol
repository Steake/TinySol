// Fibonacci Sequence Generator in TinySol
// Demonstrates complex computational capabilities

int generateFibonacci(int n) {
    int a = 0;
    int b = 1;
    int i = 0;
    
    while (i < n) {
        int temp = a;
        a = b;
        b = temp + b;
        i = i + 1;
    }
    
    return a;
}

int main() {
    int result = generateFibonacci(10);
    return result;
}
